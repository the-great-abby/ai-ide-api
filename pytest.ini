[pytest]
markers =
    unit: marks unit tests (fully mocked, no network)
    integration: marks integration tests (require multiple components)
    network: marks tests that require network access
    cli: marks CLI/argument handling tests 