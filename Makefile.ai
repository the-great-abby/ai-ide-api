# 🏴‍☠️ PIRATE WARNING: This Makefile is now a skeleton!
# Only high-level aliases and includes belong here. All real work goes in sub-Makefiles.
# If you need to add a new domain (e.g., test, db, llm, misc, admin, memory),
# create a Makefile.ai-<domain> and prefix all targets in that file with <domain>-*.
# Example: test-up, db-migrate, llm-setup, misc-lint, admin-restart, memory-add-node
#
# To run a target: make -f Makefile.ai <domain>-<target>
#
# See Makefile.ai-*, Makefile.ai-test, Makefile.ai-llm, etc. for specialized targets.

include Makefile.ai-test
include Makefile.ai-llm
include Makefile.ai-misc
include Makefile.ai-db
include Makefile.ai-admin
include Makefile.ai-memory

# High-level aliases (if any) can go here:
# Example:
# test: test-test
# lint: misc-lint

# End of captain's log.
